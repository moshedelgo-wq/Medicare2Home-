# Replace server_name only if your production hostname differs.
server {
    listen 80;
    listen [::]:80;
    server_name medicare2home.co.il www.medicare2home.co.il;
    return 301 https://$host$request_uri;
}
